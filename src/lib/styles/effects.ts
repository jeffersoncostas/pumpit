import { relativeSize } from "./scaling"

export const Elevations = {
    surface: `0px ${relativeSize(2)} ${relativeSize(
        2
    )} 0px rgba(0, 0, 0, 0.10)`,
    neon: `0px 0px ${relativeSize(5)} rgba(171, 194, 10, 0.75)`,
    button: `0px ${relativeSize(1)} ${relativeSize(2)} 0px rgba(0, 0, 0, 0.25)`,
}
